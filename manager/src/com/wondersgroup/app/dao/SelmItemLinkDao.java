package com.wondersgroup.app.dao;

import java.math.*;
import java.sql.*;
import java.util.*;

import wfc.facility.tool.autocode.*;
import wfc.service.database.*;
import org.springframework.stereotype.Repository;

import com.wondersgroup.app.bean.SelmItemLink;

/**
 * 类别关联事项
 * @author generated by wfc.facility.tool.autocode.InternalDaoGenerator
 */
@Repository
public class SelmItemLinkDao {

    private Connection con = null;

    public SelmItemLinkDao() {
    }

    public SelmItemLinkDao(Connection con) {
        this.con = con;
    }

    public void add(SelmItemLink info) {
        String sql = "insert into SELM_ITEM_LINK(ST_ITEM_ID, ST_ITEM_TYPE_ID, NM_SORT) values (?, ?, ?)";
        Object[] obj = {
            info.getStItemId(),
            info.getStItemTypeId(),
            info.getNmSort()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public void update(SelmItemLink info) {
        String sql = "update SELM_ITEM_LINK set NM_SORT = ? where ST_ITEM_ID = ? and ST_ITEM_TYPE_ID = ?";
        Object[] obj = {
            info.getNmSort(),
            info.getStItemId(),
            info.getStItemTypeId()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public int update(Map<String, Object> map, Conditions conds) {
        String sql = "update SELM_ITEM_LINK set ";
        List<Object> list = new ArrayList<Object>();
        int i = 0;
        for (String field : map.keySet()) {
            if (i++ > 0) {
                sql += ", ";
            }
            sql += field + " = ?";
            list.add(map.get(field));
        }
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            list.addAll(conds.getObjectList());
            if (con == null) {
                return SQL.execute(sql, list.toArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, list.toArray()).TOTAL_RECORD_COUNT;
            }
        }
    }
    
    public int delete(Conditions conds) {
        String sql = "delete from SELM_ITEM_LINK";
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            if (con == null) {
                return SQL.execute(sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            }
        }
    }

	public void delete(String[] stItemId, String[] stItemTypeId) {
        Conditions conds = Conditions.newOrConditions();
        for (int i = 0; i < stItemId.length; i++) {
            Conditions subconds = Conditions.newAndConditions();
            subconds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId[i]));
            subconds.add(new Condition("ST_ITEM_TYPE_ID", Condition.OT_EQUAL, stItemTypeId[i]));
            conds.add(subconds);
        }
        delete(conds);
    }

	public void delete(String stItemId, String stItemTypeId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId));
        conds.add(new Condition("ST_ITEM_TYPE_ID", Condition.OT_EQUAL, stItemTypeId));
        delete(conds);
    }
	
	public void delete(String stItemTypeId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ITEM_TYPE_ID", Condition.OT_EQUAL, stItemTypeId));
        delete(conds);
    }

    public PaginationArrayList<SelmItemLink> query(Conditions conds, String suffix, int pageSize, int currentPage) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("SELM_ITEM_LINK", "*", conds, suffix, pageSize, currentPage);
        } else {
            rs = SQL.query(con, "SELM_ITEM_LINK", "*", conds, suffix, pageSize, currentPage);
        }
        PaginationArrayList<SelmItemLink> pal = new PaginationArrayList<SelmItemLink>(rs.TOTAL_RECORD_COUNT, rs.COMMON_PAGE_SIZE, rs.CURRENT_PAGE);
        while (rs.next()) {
            SelmItemLink info = new SelmItemLink();
            setProperties(info, rs);
            pal.add(info);
        }
        return pal;
    }

    public List<SelmItemLink> query(Conditions conds, String suffix) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("SELM_ITEM_LINK", "*", conds, suffix);
        } else {
            rs = SQL.query(con, "SELM_ITEM_LINK", "*", conds, suffix);
        }
        ArrayList<SelmItemLink> al = new ArrayList<SelmItemLink>();
        while (rs.next()) {
            SelmItemLink info = new SelmItemLink();
            setProperties(info, rs);
            al.add(info);
        }
        return al;
    }
    
    public static void setProperties(SelmItemLink info, RecordSet rs){
        info.setStItemId(rs.getOriginalString("ST_ITEM_ID"));
        info.setStItemTypeId(rs.getOriginalString("ST_ITEM_TYPE_ID"));
        info.setNmSort(rs.getBigDecimal("NM_SORT"));
    }

	public SelmItemLink get(String stItemId, String stItemTypeId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId));
        conds.add(new Condition("ST_ITEM_TYPE_ID", Condition.OT_EQUAL, stItemTypeId));
        List<SelmItemLink> list = query(conds, null);
        return list.size() > 0 ? list.get(0) : null;
    }

	
}
