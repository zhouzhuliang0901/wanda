package com.wondersgroup.selmAssist.dao;

import java.math.*;
import java.sql.*;
import java.util.*;
import wfc.facility.tool.autocode.*;
import wfc.service.database.*;
import org.springframework.stereotype.Repository;

import com.wondersgroup.selmAssist.bean.SelmAssist;

/**
 * 设备辅助人员
 * @author generated by wfc.facility.tool.autocode.InternalDaoGenerator
 */
@Repository
public class SelmAssistDao {

    private Connection con = null;

    public SelmAssistDao() {
    }

    public SelmAssistDao(Connection con) {
        this.con = con;
    }

    public void add(SelmAssist info) {
        String sql = "insert into SELM_ASSIST(ST_ASSIST_ID, ST_ASSIST_NAME, ST_ASSIST_PHONE, ST_ASSIST_IDCARD, NM_ORDER, DT_CREATE, DT_UPADTE, ST_EXT1, ST_EXT2) values (?, ?, ?, ?, ?, ?, ?, ?, ?)";
        Object[] obj = {
            info.getStAssistId(),
            info.getStAssistName(),
            info.getStAssistPhone(),
            info.getStAssistIdcard(),
            info.getNmOrder(),
            info.getDtCreate(),
            info.getDtUpadte(),
            info.getStExt1(),
            info.getStExt2()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public void update(SelmAssist info) {
        String sql = "update SELM_ASSIST set ST_ASSIST_NAME = ?, ST_ASSIST_PHONE = ?, ST_ASSIST_IDCARD = ?, NM_ORDER = ?, DT_CREATE = ?, DT_UPADTE = ?, ST_EXT1 = ?, ST_EXT2 = ? where ST_ASSIST_ID = ?";
        Object[] obj = {
            info.getStAssistName(),
            info.getStAssistPhone(),
            info.getStAssistIdcard(),
            info.getNmOrder(),
            info.getDtCreate(),
            info.getDtUpadte(),
            info.getStExt1(),
            info.getStExt2(),
            info.getStAssistId()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public int update(Map<String, Object> map, Conditions conds) {
        String sql = "update SELM_ASSIST set ";
        List<Object> list = new ArrayList<Object>();
        int i = 0;
        for (String field : map.keySet()) {
            if (i++ > 0) {
                sql += ", ";
            }
            sql += field + " = ?";
            list.add(map.get(field));
        }
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            list.addAll(conds.getObjectList());
            if (con == null) {
                return SQL.execute(sql, list.toArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, list.toArray()).TOTAL_RECORD_COUNT;
            }
        }
    }
    
    public int delete(Conditions conds) {
        String sql = "delete from SELM_ASSIST";
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            if (con == null) {
                return SQL.execute(sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            }
        }
    }

	public void delete(String[] stAssistId) {
        Conditions conds = Conditions.newOrConditions();
        for (int i = 0; i < stAssistId.length; i++) {
            Conditions subconds = Conditions.newAndConditions();
            subconds.add(new Condition("ST_ASSIST_ID", Condition.OT_EQUAL, stAssistId[i]));
            conds.add(subconds);
        }
        delete(conds);
    }

	public void delete(String stAssistId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ASSIST_ID", Condition.OT_EQUAL, stAssistId));
        delete(conds);
    }

    public PaginationArrayList<SelmAssist> query(Conditions conds, String suffix, int pageSize, int currentPage) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("SELM_ASSIST", "*", conds, suffix, pageSize, currentPage);
        } else {
            rs = SQL.query(con, "SELM_ASSIST", "*", conds, suffix, pageSize, currentPage);
        }
        PaginationArrayList<SelmAssist> pal = new PaginationArrayList<SelmAssist>(rs.TOTAL_RECORD_COUNT, rs.COMMON_PAGE_SIZE, rs.CURRENT_PAGE);
        while (rs.next()) {
            SelmAssist info = new SelmAssist();
            setProperties(info, rs);
            pal.add(info);
        }
        return pal;
    }

    public List<SelmAssist> query(Conditions conds, String suffix) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("SELM_ASSIST", "*", conds, suffix);
        } else {
            rs = SQL.query(con, "SELM_ASSIST", "*", conds, suffix);
        }
        ArrayList<SelmAssist> al = new ArrayList<SelmAssist>();
        while (rs.next()) {
            SelmAssist info = new SelmAssist();
            setProperties(info, rs);
            al.add(info);
        }
        return al;
    }
    
    public static void setProperties(SelmAssist info, RecordSet rs){
        info.setStAssistId(rs.getOriginalString("ST_ASSIST_ID"));
        info.setStAssistName(rs.getOriginalString("ST_ASSIST_NAME"));
        info.setStAssistPhone(rs.getOriginalString("ST_ASSIST_PHONE"));
        info.setStAssistIdcard(rs.getOriginalString("ST_ASSIST_IDCARD"));
        info.setNmOrder(rs.getBigDecimal("NM_ORDER"));
        info.setDtCreate(rs.getTimestamp("DT_CREATE"));
        info.setDtUpadte(rs.getTimestamp("DT_UPADTE"));
        info.setStExt1(rs.getOriginalString("ST_EXT1"));
        info.setStExt2(rs.getOriginalString("ST_EXT2"));
    }

	public SelmAssist get(String stAssistId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ASSIST_ID", Condition.OT_EQUAL, stAssistId));
        List<SelmAssist> list = query(conds, null);
        return list.size() > 0 ? list.get(0) : null;
    }

}
