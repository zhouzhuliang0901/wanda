package com.wondersgroup.wdf.dao;

import java.sql.*;
import java.util.*;

import wfc.facility.tool.autocode.*;
import wfc.service.database.*;
import org.springframework.stereotype.Repository;

/**
 * 主题服务事项关联
 * @author generated by wfc.facility.tool.autocode.InternalDaoGenerator
 */
@Repository
public class UacItemsLinkTwoDao {

    private Connection con = null;

    public UacItemsLinkTwoDao() {
    }

    public UacItemsLinkTwoDao(Connection con) {
        this.con = con;
    }

    public void add(UacItemsLinkTwo info) {
        String sql = "insert into UAC_ITEMS_LINK(ST_ITEMS_ID, ST_ITEM_ID, NM_MUST, NM_ORDER) values (?, ?, ?, ?)";
        Object[] obj = {
            info.getStItemsId(),
            info.getStItemId(),
            info.getNmMust(),
            info.getNmOrder()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public void update(UacItemsLinkTwo info) {
        String sql = "update UAC_ITEMS_LINK set NM_MUST = ?, NM_ORDER = ? where ST_ITEMS_ID = ? and ST_ITEM_ID = ?";
        Object[] obj = {
            info.getNmMust(),
            info.getNmOrder(),
            info.getStItemsId(),
            info.getStItemId()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public int update(Map<String, Object> map, Conditions conds) {
        String sql = "update UAC_ITEMS_LINK set ";
        List<Object> list = new ArrayList<Object>();
        int i = 0;
        for (String field : map.keySet()) {
            if (i++ > 0) {
                sql += ", ";
            }
            sql += field + " = ?";
            list.add(map.get(field));
        }
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            list.addAll(conds.getObjectList());
            if (con == null) {
                return SQL.execute(sql, list.toArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, list.toArray()).TOTAL_RECORD_COUNT;
            }
        }
    }
    
    public int delete(Conditions conds) {
        String sql = "delete from UAC_ITEMS_LINK";
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            if (con == null) {
                return SQL.execute(sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            }
        }
    }

	public void delete(String[] stItemsId, String[] stItemId) {
        Conditions conds = Conditions.newOrConditions();
        for (int i = 0; i < stItemsId.length; i++) {
            Conditions subconds = Conditions.newAndConditions();
            subconds.add(new Condition("ST_ITEMS_ID", Condition.OT_EQUAL, stItemsId[i]));
            subconds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId[i]));
            conds.add(subconds);
        }
        delete(conds);
    }

	public void delete(String stItemsId, String stItemId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ITEMS_ID", Condition.OT_EQUAL, stItemsId));
        conds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId));
        delete(conds);
    }

    public PaginationArrayList<UacItemsLinkTwo> query(Conditions conds, String suffix, int pageSize, int currentPage) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("UAC_ITEMS_LINK", "*", conds, suffix, pageSize, currentPage);
        } else {
            rs = SQL.query(con, "UAC_ITEMS_LINK", "*", conds, suffix, pageSize, currentPage);
        }
        PaginationArrayList<UacItemsLinkTwo> pal = new PaginationArrayList<UacItemsLinkTwo>(rs.TOTAL_RECORD_COUNT, rs.COMMON_PAGE_SIZE, rs.CURRENT_PAGE);
        while (rs.next()) {
            UacItemsLinkTwo info = new UacItemsLinkTwo();
            setProperties(info, rs);
            pal.add(info);
        }
        return pal;
    }

    public List<UacItemsLinkTwo> query(Conditions conds, String suffix) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("UAC_ITEMS_LINK", "*", conds, suffix);
        } else {
            rs = SQL.query(con, "UAC_ITEMS_LINK", "*", conds, suffix);
        }
        ArrayList<UacItemsLinkTwo> al = new ArrayList<UacItemsLinkTwo>();
        while (rs.next()) {
            UacItemsLinkTwo info = new UacItemsLinkTwo();
            setProperties(info, rs);
            al.add(info);
        }
        return al;
    }
    
    public static void setProperties(UacItemsLinkTwo info, RecordSet rs){
        info.setStItemsId(rs.getOriginalString("ST_ITEMS_ID"));
        info.setStItemId(rs.getOriginalString("ST_ITEM_ID"));
        info.setNmMust(rs.getBigDecimal("NM_MUST"));
        info.setNmOrder(rs.getBigDecimal("NM_ORDER"));
    }

	public UacItemsLinkTwo get(String stItemsId, String stItemId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_ITEMS_ID", Condition.OT_EQUAL, stItemsId));
        conds.add(new Condition("ST_ITEM_ID", Condition.OT_EQUAL, stItemId));
        List<UacItemsLinkTwo> list = query(conds, null);
        return list.size() > 0 ? list.get(0) : null;
    }

}
