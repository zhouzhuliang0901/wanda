package com.wondersgroup.wdf.dao;

import java.sql.*;
import java.util.*;
import wfc.facility.tool.autocode.*;
import wfc.service.database.*;
import org.springframework.stereotype.Repository;

/**
 * 组关联人员
 * @author generated by wfc.facility.tool.autocode.InternalDaoGenerator
 */
@Repository
public class UacUserGroupLinkTwoDao {

    private Connection con = null;

    public UacUserGroupLinkTwoDao() {
    }

    public UacUserGroupLinkTwoDao(Connection con) {
        this.con = con;
    }

    public void add(UacUserGroupLinkTwo info) {
        String sql = "insert into UAC_USER_GROUP_LINK(ST_USER_ID, ST_GROUP_ID, NM_ORDER) values (?, ?, ?)";
        Object[] obj = {
            info.getStUserId(),
            info.getStGroupId(),
            info.getNmOrder()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public void update(UacUserGroupLinkTwo info) {
        String sql = "update UAC_USER_GROUP_LINK set NM_ORDER = ? where ST_USER_ID = ? and ST_GROUP_ID = ?";
        Object[] obj = {
            info.getNmOrder(),
            info.getStUserId(),
            info.getStGroupId()        	
        };
        if (con == null) {
            SQL.execute(sql, obj);
        } else {
            SQL.execute(con, sql, obj);
        }
    }

    public int update(Map<String, Object> map, Conditions conds) {
        String sql = "update UAC_USER_GROUP_LINK set ";
        List<Object> list = new ArrayList<Object>();
        int i = 0;
        for (String field : map.keySet()) {
            if (i++ > 0) {
                sql += ", ";
            }
            sql += field + " = ?";
            list.add(map.get(field));
        }
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            list.addAll(conds.getObjectList());
            if (con == null) {
                return SQL.execute(sql, list.toArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, list.toArray()).TOTAL_RECORD_COUNT;
            }
        }
    }
    
    public int delete(Conditions conds) {
        String sql = "delete from UAC_USER_GROUP_LINK";
        String subsql = conds != null ? conds.toString() : "";
        if ("".equals(subsql)) {
            if (con == null) {
                return SQL.execute(sql).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql).TOTAL_RECORD_COUNT;
            }
        } else {
            sql += " where " + subsql;
            if (con == null) {
                return SQL.execute(sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            } else {
                return SQL.execute(con, sql, conds.getObjectArray()).TOTAL_RECORD_COUNT;
            }
        }
    }

	public void delete(String[] stUserId, String[] stGroupId) {
        Conditions conds = Conditions.newOrConditions();
        for (int i = 0; i < stUserId.length; i++) {
            Conditions subconds = Conditions.newAndConditions();
            subconds.add(new Condition("ST_USER_ID", Condition.OT_EQUAL, stUserId[i]));
            subconds.add(new Condition("ST_GROUP_ID", Condition.OT_EQUAL, stGroupId[i]));
            conds.add(subconds);
        }
        delete(conds);
    }

	public void delete(String stUserId, String stGroupId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_USER_ID", Condition.OT_EQUAL, stUserId));
        conds.add(new Condition("ST_GROUP_ID", Condition.OT_EQUAL, stGroupId));
        delete(conds);
    }

    public PaginationArrayList<UacUserGroupLinkTwo> query(Conditions conds, String suffix, int pageSize, int currentPage) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("UAC_USER_GROUP_LINK", "*", conds, suffix, pageSize, currentPage);
        } else {
            rs = SQL.query(con, "UAC_USER_GROUP_LINK", "*", conds, suffix, pageSize, currentPage);
        }
        PaginationArrayList<UacUserGroupLinkTwo> pal = new PaginationArrayList<UacUserGroupLinkTwo>(rs.TOTAL_RECORD_COUNT, rs.COMMON_PAGE_SIZE, rs.CURRENT_PAGE);
        while (rs.next()) {
            UacUserGroupLinkTwo info = new UacUserGroupLinkTwo();
            setProperties(info, rs);
            pal.add(info);
        }
        return pal;
    }

    public List<UacUserGroupLinkTwo> query(Conditions conds, String suffix) {
        RecordSet rs;
        if (con == null) {
            rs = SQL.query("UAC_USER_GROUP_LINK", "*", conds, suffix);
        } else {
            rs = SQL.query(con, "UAC_USER_GROUP_LINK", "*", conds, suffix);
        }
        ArrayList<UacUserGroupLinkTwo> al = new ArrayList<UacUserGroupLinkTwo>();
        while (rs.next()) {
            UacUserGroupLinkTwo info = new UacUserGroupLinkTwo();
            setProperties(info, rs);
            al.add(info);
        }
        return al;
    }
    
    public static void setProperties(UacUserGroupLinkTwo info, RecordSet rs){
        info.setStUserId(rs.getOriginalString("ST_USER_ID"));
        info.setStGroupId(rs.getOriginalString("ST_GROUP_ID"));
        info.setNmOrder(rs.getBigDecimal("NM_ORDER"));
    }

	public UacUserGroupLinkTwo get(String stUserId, String stGroupId) {
        Conditions conds = Conditions.newAndConditions();
        conds.add(new Condition("ST_USER_ID", Condition.OT_EQUAL, stUserId));
        conds.add(new Condition("ST_GROUP_ID", Condition.OT_EQUAL, stGroupId));
        List<UacUserGroupLinkTwo> list = query(conds, null);
        return list.size() > 0 ? list.get(0) : null;
    }

}
